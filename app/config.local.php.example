<?php
declare(strict_types=1);

/**
 * Copy this file to app/config.local.php and fill in real values.
 *
 * config.local.php is gitignored. Never put credentials in app/config.php.
 * Only the keys you want to override need to appear here.
 */
return [
    'site' => [
        'domain' => 'example.ir',
        'scheme' => 'https',
    ],

    'db' => [
        'host' => 'localhost',
        'name' => 'cpaneluser_recipes',
        'user' => 'cpaneluser_recipes',
        'pass' => 'CHANGE-ME',
    ],

    'worker' => [
        // The shared secret the VPS worker signs its requests with.
        // Generate with: php -r "echo bin2hex(random_bytes(32));"
        'hmac_secret' => 'CHANGE-ME',
        'ip_allowlist' => ['203.0.113.10'],   // your VPS public IP
    ],

    'security' => [
        // Generate with: php -r "echo bin2hex(random_bytes(32));"
        'app_key' => 'CHANGE-ME',
        // Keys stored phone numbers. Generate the same way, set it once, and
        // never change it: a new pepper makes every contributor unknown.
        'phone_pepper' => 'CHANGE-ME',
    ],

    // SMS sign-in codes. Pick one gateway, create an OTP template in its
    // panel, and fill in its key. See docs/DEPLOY.md.
    'sms' => [
        'driver' => 'kavenegar',
        'kavenegar' => ['api_key' => '', 'template' => ''],
        // 'smsir'    => ['api_key' => '', 'template_id' => 0, 'parameter' => 'CODE'],
        // 'ghasedak' => ['api_key' => '', 'template' => ''],
    ],

    'debug' => false,
];
