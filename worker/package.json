{
  "name": "recipe-research-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deep-research worker. Runs on a VPS outside Iran, pulls jobs from the site, and pushes finished Persian drafts back.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/index.js",
    "once": "node src/index.js --once",
    "pipeline": "node src/index.js --local",
    "test": "node --test test/"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "jsdom": "^24.0.0"
  }
}
